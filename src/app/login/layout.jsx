export const metadata = {
  title: "Login",
  description: "Sign in to your InkVerse account to share your words, code, and soul with the world.",
};

export default function LoginLayout({ children }) {
  return children;
}
