export const metadata = {
  title: "Join InkVerse",
  description: "Create your InkVerse account today and start sharing code snippets, poetry, and quotes with a global community.",
};

export default function SignupLayout({ children }) {
  return children;
}
