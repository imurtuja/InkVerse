export const metadata = {
  title: "Explore Feed",
  description: "Browse the latest code snippets, poetry, and quotes from the InkVerse community.",
};

export default function FeedLayout({ children }) {
  return children;
}
