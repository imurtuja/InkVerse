// Service Worker for InkVerse
// This file is currently a placeholder to resolve 404 errors.
// PWA features can be implemented here in the future.

self.addEventListener('install', () => {
  self.skipWaiting();
});

self.addEventListener('activate', () => {
  // Clear old caches if any
});

self.addEventListener('fetch', () => {
  // Pass through all requests
});
